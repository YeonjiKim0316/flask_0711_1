# 0713 요구사항 (~10시까지)

- 1) 파일 정리 - 불필요한 파일 삭제 및 .gitignore 에 숨길 파일 목록 재작성 v
- 2) 질문과 관련된 기능을 question_views.py로 변경하고 관련 MVC 파트도 모두 변경합니다. v
    - question = Blueprint('question', __name__, url_prefix='/question')
- 3) 네비게이션 바의 항목을 아래와 같이 변경하세요(원래 있던 Home, Developer, Technical Editor 항목은 삭제합니다)
    - 가. QnA라는 이름으로 된 question_list.html로 가는 경로 추가(이미 있는 주소를 활용하세요)
    - 나. 부트스트랩을 활용하여 SignUp, Login 버튼을 만들어주세요.


# answer_views 구현
- answer_set 이라는 backref를 통해 question을 통해 answer_id를 역참조 할 수 있음

# pagination
- flask shell에서 임시데이터 100개 생성 후 화면 레이아웃 구성

>>> for i in range(100):
...     q = Question(subject="제목:[%03d]" % i, content="글내용", create_date=datetime.now())
...     db.session.add(q)
...
...                    
>>> db.session.commit()
>>> q = Question.query.all()
>>> q

# 커스텀 필터 만들기
1) filter.py에 필터 등록 -
2) __init__에 필터의 변수명, 필터에서 사용할 filter.py의 함수를 import 
3) Template에서 해당 필터를 {{ 변수명 | 필터명}} 으로 사용 
# question_detail.html 에서 사용할 시간 필터를 만들어서 직접 화면에 출력해주세요. 
# date_time2 필터명을 작성해서 등록합니다. 

# https://docs.python.org/ko/3/library/datetime.html


# 회원가입 기능을 관리하는 MTV 작성
    회원 
- 가입
    M - 회원에 관한 Model -> Table
    T - signup.html
    V - /signup/


- 회원 관리
    M - 회원에 관한 Model -> Table
    T - id 비밀번호 확인하는
        login.html
    V - /login/
    		auth_views.py 만들어서 __init__에 등록합니다. 'auth' 


    @login required 
    {% if logout % }
        로그아웃 
        - signup, Login 버튼 둘다보인다
    {% else %}
        인증 확인 
    로그인한 화면 
        - if 인증된 사람에게만 보여주는 로그아웃 버튼 
    {% endif %}